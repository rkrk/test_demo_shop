
<% @order_history.each do |oh| %>
  <ul>
    <%= link_to "#{oh.created_at}", {:controller => "store" ,:action => "show_lineitem_history",
                                     :order_id => "#{oh.id}"}%>
  </ul>
<% end %>

